# ============================================================
# Залежності проєкту: Виявлення фейкових новин
# Трансформерні моделі: BERT, DistilBERT, RoBERTa
# ============================================================
# Встановлення: pip install -r requirements.txt
# ============================================================

# --- Основний фреймворк глибокого навчання ---
torch==2.1.2                  # PyTorch — базовий фреймворк
torchvision==0.16.2           # Необхідний допоміжний пакет для torch

# --- Трансформери та NLP ---
transformers==4.37.2          # HuggingFace: BERT, DistilBERT, RoBERTa
tokenizers==0.15.1            # Швидкі токенізатори (Rust-based)
datasets==2.16.1              # Зручна робота з датасетами HuggingFace

# --- Робота з даними ---
pandas==2.1.4                 # Обробка CSV, DataFrame операції
numpy==1.26.3                 # Числові обчислення
scikit-learn==1.3.2           # Метрики, train_test_split, confusion matrix

# --- Візуалізація ---
matplotlib==3.8.2             # Графіки втрат та точності
seaborn==0.13.1               # Красиві heatmap для confusion matrix
wordcloud==1.9.3              # Хмари слів для EDA
Pillow==10.2.0                # Залежність для wordcloud

# --- Прогрес-бар та утиліти ---
tqdm==4.66.1                  # Прогрес-бар під час навчання
accelerate==0.26.1            # Оптимізація навчання (HuggingFace)

# --- Збереження та серіалізація ---
safetensors==0.4.1            # Безпечний формат збереження ваг моделей

# --- Jupyter (опціонально, для ноутбуків) ---
jupyter==1.0.0
ipykernel==6.29.0
ipywidgets==8.1.1
